name: 'Digital Wallet 2'
on:
  workflow_dispatch:
  push:
    branches:
      - ci
jobs:
  check-application:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: 1.18
      - run: make cover